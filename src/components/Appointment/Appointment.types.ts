// Interfaces and types from component Appointment
import { Appointment } from "../../interfaces/appointment.types";

// Component Props
export interface AppointmentProps extends Appointment {}

// Styled Component Props
export interface AppointmentStyledProps {
  className: string;
}
