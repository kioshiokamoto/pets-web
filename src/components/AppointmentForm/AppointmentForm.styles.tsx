import styled from "styled-components";

import { AppointmentFormStyledProps as Props } from "./AppointmentForm.types";

const AppointmentFormStyled = styled.div<Props>`
  .AppointmentForm {
  }
`;

export default AppointmentFormStyled;
